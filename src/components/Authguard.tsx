// import * as React from 'react';
// import { Redirect, Route, RouteProps } from 'react-router-dom';

// const AuthGuard = ({ component, ...rest }: RouteProps) => (
//   <Route
//     {...rest}
//     render={props =>
//       fakeAuth.isAuthenticated ? (
//         <Component {...props} />
//       ) : (
//         <Redirect
//           to={{
//             pathname: '/login',
//             state: { from: props.location }
//           }}
//         />
//       )
//     }
//   />
// );
