import { AnyAction } from 'redux';

const initState = {};

const player = (state = initState, action: AnyAction) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default player;
